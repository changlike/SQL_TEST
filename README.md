### PROGRAMMERS SQL 기출 문제 풀이 

[기본 문법]
JOIN : 여러 테이블을 결합하여 원하는 데이터를 조회
ON : 두 테이블을 어떤 컬럼으로 연결할지 지정
IN : 서브쿼리 결과 목록에 포함되는 값만 조회
서브쿼리 : 괄호 안의 SELECT문으로, 먼저 실행되어 결과를 반환
WHERE : 행(Row)을 조건에 따라 필터링
ORDER BY : 조회 결과를 정렬
DESC : 내림차순 정렬 (큰 값 → 작은 값)

